<UserControl x:Class="ClientDesktop.View.Details.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ClientDesktop.ViewModel;assembly=ClientDesktop.ViewModel"
             xmlns:conv="clr-namespace:ClientDesktop.View.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="1000">

    <UserControl.Resources>
        <conv:ProfitToColorConverter x:Key="ProfitColorConverter"/>
        <conv:OrderTypeToColorConverter x:Key="OrderTypeColorConverter"/>
        <conv:VolumeFormatConverter x:Key="VolumeFormatConverter"/>
        <Style x:Key="RightAlignCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <DataGrid ItemsSource="{Binding PositionCollectionView}"
                  Style="{StaticResource CustomDataGrid}"  
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  BorderThickness="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserSortColumns="True"
                  Sorting="DataGrid_Sorting"
                  SelectionUnit="FullRow">

            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
                      Command="{Binding DoubleClickCommand}"
                      CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
            </DataGrid.InputBindings>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">

                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="Normal"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridRow">
                                <Border x:Name="DGR_Border"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        SnapsToDevicePixels="True">
                                    <Grid>
                                        <DataGridCellsPresenter x:Name="CellsPresenter"
                                                                ItemsPanel="{TemplateBinding ItemsPanel}"
                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                        <Grid x:Name="FooterPanel" Visibility="Collapsed" Height="25">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="{Binding SymbolName}" 
                                                       Grid.Column="0"
                                                       VerticalAlignment="Center"
                                                       Margin="5,0,0,0"
                                                       FontWeight="Bold"/>

                                            <TextBlock x:Name="FooterPnlText"
                                                       Text="{Binding Pnl, StringFormat=F2}" 
                                                       Grid.Column="1"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,10,0"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Pnl, Converter={StaticResource ProfitColorConverter}}"/>
                                        </Grid>
                                    </Grid>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#EEEEEE"/>
                                    </Trigger>

                                    <DataTrigger Binding="{Binding RefId}" Value="FOOTER">
                                        <Setter TargetName="CellsPresenter" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="FooterPanel" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#D3D3D3"/>
                                    </DataTrigger>

                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#0078D7"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>

                                    <DataTrigger Binding="{Binding Type}" Value="Footer">
                                        <Setter TargetName="CellsPresenter" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="FooterPanel" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#E0E0E0"/>
                                    </DataTrigger>

                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Type}" Value="Footer"/>
                                            <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#E0E0E0"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </MultiDataTrigger>

                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Type}" Value="Position"/>
                                            <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#0078D7"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </MultiDataTrigger>

                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Type}" Value="Order"/>
                                            <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="DGR_Border" Property="Background" Value="#0078D7"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </MultiDataTrigger>

                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Symbol" Width="2.5*" SortMemberPath="SymbolName" CanUserSort="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding SymbolName}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="5,0,0,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat='{}{0:dd/MM/yy HH:mm}'}" ElementStyle="{StaticResource RightAlignCell}" Width="1.5*" SortMemberPath="Time" CanUserSort="True"/>

                <DataGridTemplateColumn Header="Type" Width="0.8*" SortMemberPath="Side" CanUserSort="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Side}" VerticalAlignment="Center" Margin="5,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding Side, Converter={StaticResource OrderTypeColorConverter}}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Order Type" Binding="{Binding OrderType}" Width="1*" SortMemberPath="OrderType" CanUserSort="False"/>
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume, Converter={StaticResource VolumeFormatConverter}}" ElementStyle="{StaticResource RightAlignCell}" Width="0.8*" SortMemberPath="Volume" CanUserSort="False"/>
                <DataGridTextColumn Header="Avg Price" Binding="{Binding AveragePriceDisplay}" ElementStyle="{StaticResource RightAlignCell}" Width="1*" SortMemberPath="AveragePrice"/>
                <DataGridTextColumn Header="Price" Binding="{Binding CurrentPriceDisplay}" Width="1*" CanUserSort="False">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding PriceColor, FallbackValue=Black}"/>
                            <Setter Property="TextAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,5,0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Floating P/L" Width="1.2*" SortMemberPath="Pnl" CanUserSort="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding PnlDisplay}"
                       VerticalAlignment="Center"
                       Margin="5,0,5,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="Foreground" Value="{Binding PnlColor, FallbackValue=Black}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsOrder}" Value="True">
                                                <Setter Property="TextAlignment" Value="Left"/>
                                                <Setter Property="Foreground" Value="Black"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>