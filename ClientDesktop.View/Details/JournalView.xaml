<UserControl x:Class="ClientDesktop.View.Details.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ClientDesktop.ViewModel;assembly=ClientDesktop.ViewModel">

    <UserControl.DataContext>
        <vm:JournalViewModel />
    </UserControl.DataContext>

    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Journals}" AutoGenerateColumns="False" 
                  GridLinesVisibility="All" HeadersVisibility="Column" BorderThickness="0"
                  Style="{StaticResource CustomDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat='{}{0:dd-MM-yyyy HH:mm:ss}'}" Width="150"/>
                <DataGridTextColumn Header="Dealer" Binding="{Binding Dealer}" Width="120"/>
                <DataGridTextColumn Header="Login" Binding="{Binding Login}" Width="100"/>
                <DataGridTextColumn Header="Request" Binding="{Binding Request}" Width="200"/>
                <DataGridTextColumn Header="Answer" Binding="{Binding Answer}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="No data available" 
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"
                   FontSize="16" Foreground="Gray">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Journals.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>