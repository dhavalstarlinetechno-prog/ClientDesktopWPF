<UserControl x:Class="ClientDesktop.View.Details.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFF" Padding="0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="0.6*"/>
                    <ColumnDefinition Width="0.6*"/>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Grid.ColumnSpan="3" 
                           Text="Deals" Style="{StaticResource HeaderLabel}" 
                           FontSize="14" Margin="10,0,0,0" Foreground="Black"/>

                <ComboBox x:Name="FilterSymbol" Grid.Column="3" Margin="1" Text="All" IsEnabled="False">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="EURUSD"/>
                    <ComboBoxItem Content="XAUUSD"/>
                </ComboBox>

                <ComboBox x:Name="FilterExecution" Grid.Column="4" Margin="1" Text="All" IsEnabled="False">
                    <ComboBoxItem Content="All"/>
                </ComboBox>

                <ComboBox x:Name="FilterType" Grid.Column="5" Margin="1" Text="All" IsEnabled="False">
                    <ComboBoxItem Content="All"/>
                </ComboBox>

                <ComboBox x:Name="FilterEntry" Grid.Column="6" Margin="1" Text="All" IsEnabled="False">
                    <ComboBoxItem Content="All"/>
                </ComboBox>

                <StackPanel Grid.Column="7" Grid.ColumnSpan="5" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            Margin="0,0,10,0">

                    <ComboBox x:Name="PeriodCombo" Width="120" Margin="5,0"
                              SelectionChanged="PeriodCombo_SelectionChanged">
                        <ComboBoxItem Content="Today"/>
                        <ComboBoxItem Content="Last 3 Days"/>
                        <ComboBoxItem Content="Last Week"/>
                        <ComboBoxItem Content="Last Month"/>
                        <ComboBoxItem Content="Last 3 Months"/>
                        <ComboBoxItem Content="Last 6 Months"/>
                        <ComboBoxItem Content="All History"/>
                    </ComboBox>

                    <DatePicker x:Name="StartDatePicker" Width="100"/>
                    <TextBlock Text="-" Style="{StaticResource HeaderLabel}"/>
                    <DatePicker x:Name="EndDatePicker" Width="100"/>

                    <Button Content="Request" Width="70" Margin="5,0,5,0" 
                            Background="#5D9CEC" Foreground="White" BorderThickness="0"
                            Click="RequestButton_Click"/>

                    <Button Content="📊" ToolTip="Excel" Width="25" Background="#27AE60" Foreground="White" Margin="1,0"/>
                    <Button Content="📄" ToolTip="PDF" Width="25" Background="#C0392B" Foreground="White" Margin="1,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <DataGrid x:Name="HistoryGrid"
                      Style="{StaticResource CustomDataGrid}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      BorderThickness="0"
                      RowHeaderWidth="0">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#4682B4"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding CreatedOn, StringFormat='{}{0:yyyy.MM.dd HH:mm}'}" Width="1.5*"/>
                    <DataGridTextColumn Header="Deal Id" Binding="{Binding ClientDealId}" Width="*"/>
                    <DataGridTextColumn Header="Position Id" Binding="{Binding PositionId}" Width="*"/>
                    <DataGridTextColumn Header="Symbol" Binding="{Binding SymbolName}" Width="*" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Execution" Binding="{Binding OrderType}" Width="0.8*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Side}" Width="0.6*"/>
                    <DataGridTextColumn Header="Entry" Binding="{Binding DealType}" Width="0.6*"/>
                    <DataGridTextColumn Header="Volume" Binding="{Binding Volume}" Width="0.8*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
                    <DataGridTextColumn Header="Comm." Binding="{Binding Fee}" Width="0.8*"/>
                    <DataGridTextColumn Header="Profit" Binding="{Binding Pnl}" Width="*" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="NoDataLabel" 
                       Text="No data available" 
                       FontSize="24" 
                       Foreground="Gray"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Visibility="Visible"/>
        </Grid>
    </Grid>
</UserControl>