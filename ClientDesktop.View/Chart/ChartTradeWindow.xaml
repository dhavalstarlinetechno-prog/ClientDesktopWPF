<UserControl x:Class="ClientDesktop.View.Chart.ChartTradeWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:ClientDesktop.View.Chart"
             mc:Ignorable="d" 
             Background="#ffffff"
             Loaded="ChartTradeWindow_Loaded"
             Unloaded="ChartTradeWindow_Unloaded">
    <Grid>
        <wv2:WebView2 x:Name="ChartWebView" Visibility="Visible"/>

        <Grid x:Name="LoadingOverlay" Background="#ffffff">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding ChartTitle}"
                   Foreground="#555577"
                   FontSize="20"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"/>

                <TextBlock x:Name="LoadingText"
                   Text="Loading Dynamic Chart..."
                   Foreground="#444466"
                   FontSize="13"
                   Margin="0,8,0,0"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   MaxWidth="400"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
