<UserControl x:Class="ClientDesktop.View.Disclaimer.DisclaimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="350" Height="400"
             Background="Transparent">

    <!-- Outer shadow / Border -->
    <Border CornerRadius="6"
            Background="White"
            Padding="0"
            Margin="5"
            SnapsToDevicePixels="True">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Header -->
            <Border Grid.Row="0"
                    Background="#F5F5F5"
                    Padding="10"
                    CornerRadius="6,6,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Disclaimer"
                               FontSize="14"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Foreground="#222"/>

                    <!-- Close Button bound to ViewModel Command -->
                    <Button Grid.Column="1"
                            Width="24" Height="24"
                            ToolTip="Close"
                            Cursor="Hand"
                            Command="{Binding CloseCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#555"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                <TextBlock Text="✕" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <!-- Hover state - Windows Red -->
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E81123"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                                <!-- Pressed state - Darker Red -->
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#C2111D"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1"
                          Margin="12"
                          VerticalScrollBarVisibility="Auto">
                <TextBlock TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="#333" TextAlignment="Justify">
                    <Run Text="This application is developed solely for educational purposes. It is intended to provide users with a basic understanding of financial instruments, trading concepts, and market strategies. No real money is involved, and this app does not support or facilitate any real-world trading or financial transactions."/>
                    <LineBreak/><LineBreak/>
                    <Run Text="All data, prices, and simulations presented within the app are purely fictitious or for demonstration purposes and may not reflect real market conditions. Users are advised not to rely on the content for actual investment decisions hints."/>
                    <LineBreak/><LineBreak/>
                    <Run Text="This software is completely free and provided as a learning tool. We do not promote or encourage any specific financial product, instrument, or service. Use of the app is entirely at the user's discretion, and we are not responsible for any financial decisions made outside this platform."/>
                </TextBlock>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2"
                    BorderThickness="0,0,0,0"
                    BorderBrush="#DDD"
                    Padding="10">
                <!-- Acknowledge Button updated to use CommonActionButtonStyle and trigger on Enter -->
                <Button Content="I acknowledge"
                        Width="120"
                        Height="30"
                        HorizontalAlignment="Center"
                        Background="#42A5F5"
                        Foreground="White"
                        Style="{StaticResource CommonActionButtonStyle}"
                        IsDefault="True"
                        Command="{Binding AcknowledgeCommand}"/>
            </Border>
        </Grid>
    </Border>
</UserControl>