<UserControl x:Class="ClientDesktop.View.Navigation.BanScript"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientDesktop.View.Navigation"
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="320">
            <Border BorderBrush="Gray"
            BorderThickness="1"
            Padding="6">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal"
                     Margin="0,0,0,8">
                <!--<Image x:Name="Banscriptimage"
                    Width="24"
                    Height="24"
                    Stretch="Uniform"/>-->

                <Viewbox Width="24" Height="24">
                    <Path
                    Data="M4 2C2.9 2 2 2.9 2 4V20C2 21.1 2.9 22 4 22H20C21.1 22 22 21.1 22 20V8L16 2H4Z
                    M14 3.5L20.5 10H14V3.5Z
                    M8.7 12.3L11 14.6L13.3 12.3L14.7 13.7L12.4 16L14.7 18.3L13.3 19.7L11 17.4L8.7 19.7L7.3 18.3L9.6 16L7.3 13.7L8.7 12.3Z"
                    Fill="CornflowerBlue"
                    Stretch="Uniform"/>
                </Viewbox>

                <TextBlock Text="Ban Script"
                        FontSize="15"
                        FontWeight="Bold"
                        VerticalAlignment="Center"
                        Margin="8,0,0,0"/>
            </StackPanel>

            <!-- Date -->
            <WrapPanel Grid.Row="1"
                    Margin="0,0,0,6">
                <TextBlock Text="Today's Banscripts Data"
                        FontSize="12"/>

                <TextBlock x:Name="LblBanscripiptdate"
                        FontSize="12"
                        Margin="5,0,0,0"/>
            </WrapPanel>

            <!-- Grid -->
            <Grid Grid.Row="2">
                <DataGrid x:Name="DgvBanScript"
                       ItemsSource="{Binding GridRows}"
                       Style="{StaticResource CustomDataGrid}"   
                       AutoGenerateColumns="False"
                       IsReadOnly="True"
                       CanUserSortColumns="False"
                       SelectionMode="Single"
                       HeadersVisibility="Column"
                       GridLinesVisibility="All"
                       BorderThickness="0"
                       SelectionUnit="FullRow"
                       CanUserAddRows="False"
                       CanUserDeleteRows="False"                      
                       VerticalScrollBarVisibility="Auto"
                       IsHitTestVisible="False">
                                                                            
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Header="Symbol" FontFamily="Microsoft Sans Serif"
                            Binding="{Binding Symbol}"
                            Width="*"/>
                    </DataGrid.Columns>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="CornflowerBlue"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Regular"/>
                            <Setter Property="FontSize" Value="13"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>

                <!-- No Data -->
                <TextBlock x:Name="LblNoData"
                        Text="No Data Available"
                        FontSize="15"                                   
                        FontWeight="Bold"   
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" 
                        FontFamily="Microsoft Sans Serif">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>

                                <!-- Show only after data loaded AND no rows -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsDataLoaded}" Value="True"/>
                                        <Condition Binding="{Binding GridRows.Count}" Value="0"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>

                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

            </Grid>
        </Grid>

    </Border>
</UserControl>
